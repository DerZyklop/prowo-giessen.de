title: Seite mit Unter-Kapiteln

files: false

pages:
  template: default

options:
  template: event
  status: false

fields:
  title:
    label: Title
    type: title
    required: true
    width: 3/4

  perpage:
    label: Posts per page
    type: number
    min: 1
    max: 20
    default: 5
    width: 1/4

  text:
    label: Text
    type: textarea
